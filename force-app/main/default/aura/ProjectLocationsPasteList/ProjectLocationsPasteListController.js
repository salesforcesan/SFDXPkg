({
    onInit: function(cmp, evt, helper) {
        helper.onInit(cmp, evt);
    },
    
    add: function(cmp, evt, helper) {
        helper.add(cmp, evt);
    },
    
    cancel: function(cmp, evt, helper) {
        helper.cancel(cmp, evt);
    },
    
    onSuccess: function(cmp, evt, helper) {
        helper.onSuccess(cmp, evt);
    },
    
    onError: function(cmp, evt, helper) {
        helper.onError(cmp, evt);
    },
    
    export: function(cmp, evt, helper) {
        helper.export(cmp, evt);
    },
    goBack: function(cmp, evt, helper) {
        helper.goBack(cmp, evt);
    }
    
})