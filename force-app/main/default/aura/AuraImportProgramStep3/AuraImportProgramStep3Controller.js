({
  onInit: function(cmp, evt, h) {
    h.init(cmp);
  },

  onInitUploadFile: function(cmp, evt, h) {
    h.initUploadFile(cmp, evt);
  },

  onUploadFile: function(cmp, evt, h) {
    h.uploadFile(cmp, evt);
  }
});