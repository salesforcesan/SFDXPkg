({
  doInit: function(cmp, evt, helper) {
    helper.init(cmp);
  },

  doShow: function(cmp, evt, helper) {
    helper.show(cmp);
  },

  doHide: function(cmp, evt, helper) {
    helper.hide(cmp);
  }
})