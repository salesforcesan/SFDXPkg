({
    onShowPhotoDetail: function(cmp, evt, h){
        h.showPhotoDetail(cmp, evt);
    },

    onClosePhotoDetail: function(cmp, evt, h){
        h.closePhotoDetail(cmp, evt);
    },

    onPhotoClick: function(cmp, evt, h){
        h.photoClick(cmp, evt);
    },

    onMarkFavoriteEventRaised: function(cmp, evt, h){
        h.markFavoriteEventRaised(cmp,evt);
    },

})